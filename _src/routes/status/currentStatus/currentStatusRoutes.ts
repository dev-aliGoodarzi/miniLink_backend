// Express
import express from "express";
// Express

import { Systeminformation } from "systeminformation";

export const currentStatusRoutes = express.Router();

currentStatusRoutes.get("/current-status", (req, res) => {});
